import './App.css';

function App() {
  return (
 <>
 <h1>Using create-react-app as Example</h1>
 <h4>With npx (no global install needed):</h4>
 <p>
  npx downloads and runs the latest version of create-react-app directly from npm.
  No need to install anything globally.
  After the command finishes, create-react-app is removed (unless already installed locally).
  </p>

</>
);
}

export default App;
